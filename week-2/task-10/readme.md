# 🔥 День 10. Управление контекстом: разные стратегии (без summary)

## Задание

Реализуйте в агенте 3 разные стратегии управления контекстом (минимум) и переключатель между ними:

- 👉 **Стратегия 1 — Sliding Window:** храните только последние N сообщений, остальное отбрасывайте.
- 👉 **Стратегия 2 — Sticky Facts / Key-Value Memory:** отдельный блок `facts` (ключ→значение) с важными данными (цель, ограничения, предпочтения, решения, договорённости); обновляйте facts после каждого сообщения пользователя; в запрос — facts + последние N.
- 👉 **Стратегия 3 — Branching:** checkpoint в диалоге, 2 ветки от одного места, независимое продолжение, переключение между ветками.

Протестируйте на одном сценарии («собираем ТЗ за 10–15 сообщений»), прогоните на каждой стратегии, сравните: **качество ответа · стабильность (не теряет ли важные детали) · расход токенов · удобство.**

## Результат

Агент с 3 стратегиями (Sliding Window / Facts / Branching) + сравнение результатов.

## Формат

Видео + Код

---

## Архитектура: переключаемая стратегия

Контекст строит **стратегия**, а не сам агент. Интерфейс:

```go
type Strategy interface {
Name() string
Prepare(ctx, userInput) (system []TextBlockParam, msgs []MessageParam, err error)
Commit(assistant string)
}
```

`Prepare` принимает новое сообщение пользователя, обновляет состояние стратегии и
возвращает то, что реально уйдёт в модель. `Commit` сохраняет ответ. Агент дёргает
`Prepare → вызов модели → Commit` и не знает деталей. Переключатель — `agent.SetStrategy(...)`
или флаг `-strategy`.

### Стратегия 1 — Sliding Window

Буфер последних N сообщений; всё старше N отбрасывается (`lastN`). Просто и дёшево,
но к концу длинного диалога ранние детали **теряются** — окно их не видит.

### Стратегия 2 — Sticky Facts

`facts map[string]string` — key-value важных данных. После каждого сообщения
пользователя дешёвая модель (**Haiku**) обновляет facts (возвращает JSON ключ→значение,
парсится в map). В запрос идёт блок facts (system) + последние N сообщений. Окно
маленькое, но **важное не теряется** — оно в facts. Накладные — служебные вызовы
Haiku (учитываются отдельно).

### Стратегия 3 — Branching

`branches map[string][]message`, активная ветка `current`. `Branch(name)` форкает
текущую историю (checkpoint), `Switch(name)` переключает. Каждая ветка продолжается
**независимо** и помнит свой выбор. Не про экономию токенов — про параллельные
A/B-сценарии от общего начала.

---

## Два режима

### Отчёт — сравнение стратегий на сценарии «собираем ТЗ»

```bash
go run . -report
```

Прогоняет один сценарий (11 сообщений: стек, требования, дедлайн → «собери всё ТЗ»)
на Sliding Window и Sticky Facts, считает **стабильность** (сколько из 10 ключевых
фактов попало в финальное ТЗ) и токены, плюс показывает Branching-демо (2 ветки,
каждая помнит свою базу данных).

### Интерактивный чат с выбором стратегии

```bash
go run . -strategy facts       # sliding | facts | branching
```

Для Branching доступны команды чата: `/branch <имя>` — форк, `/switch <имя>` — переключить ветку.

---

## Пример вывода `-report` (реальный прогон, полностью)

> Полный вывод программы: реплики сценария, **полные ТЗ** обеих стратегий,
> чек-листы фактов, накопленные facts, демо Branching и блок сравнения.

```
% go run ./week-2/task-10 -report
=== День 10: стратегии управления контекстом (без summary) ===
Сценарий «собираем ТЗ» (11 сообщений), окно N=4.
Реплики пользователя (один и тот же сценарий для всех стратегий):
   1. Делаем сервис order-svc на Go.
   2. База данных — Postgres.
   3. Очередь сообщений — Kafka.
   4. Нужен rate limiting: 100 запросов в секунду на клиента.
   5. Авторизация — по JWT, токен живёт 15 минут.
   6. Логи структурные, в JSON.
   7. Деплой в Kubernetes, 3 реплики.
   8. Метрики через Prometheus.
   9. Дедлайн MVP — 1 марта.
  10. Тестовое покрытие — не меньше 70%.
  11. Собери всё ТЗ воедино: стек, требования и дедлайн.

# Стратегия 1: Sliding Window
Финальное ТЗ (полный ответ модели):
----------------------------------------
Собираю всё, что мы зафиксировали. Но сразу честно помечу **пробелы** — по части пунктов мы не договаривались, и я их не выдумываю, а выношу в открытые вопросы.

## Что мы реально зафиксировали в диалоге

1. **Дедлайн MVP:** 1 марта.
2. **Покрытие тестами:** не меньше 70% (как CI-gate), с осознанным покрытием бизнес-логики и auth.
3. **Обсуждавшиеся компоненты:** HTTP-слой, JWT-аутентификация, rate limiting, метрики, graceful shutdown, деплой в K8s.
4. **Спорный момент:** изначально закладывались 3 реплики + Redis-лимитер, но для MVP предложено упростить до 1 реплики + in-memory лимитер.

Всё остальное ниже — это **мои предложения по умолчанию**, а не ваши требования. Проверьте их.

---

# ТЗ: Backend-сервис (MVP)

## 1. Стек (требует вашего подтверждения)

> ⚠️ Язык **Go** я вывел из примеров кода в обсуждении. Версию, фреймворк и БД мы **не обсуждали** — ниже дефолтные предложения.

| Компонент | Предложение | Статус |
|-----------|-------------|--------|
| Язык | Go (1.22+) | выведено из контекста |
| HTTP | stdlib `net/http` или chi | **не обсуждалось** |
| БД | ? | **не обсуждалось** |
| Auth | JWT | ✅ зафиксировано |
| Rate limiting | in-memory (MVP) → Redis (потом) | ✅ обсуждалось |
| Метрики | Prometheus `/metrics` | ✅ обсуждалось |
| Оркестрация | Kubernetes | ✅ обсуждалось |

## 2. Функциональные требования

> ⚠️ **Главный пробел:** что именно делает сервис (предметная область, сущности, эндпоинты) — мы **не обсуждали**. Без этого ТЗ неполное.

- [ ] Основная бизнес-логика — **требует описания от вас**
- [x] JWT-аутентификация (valid/expired/no-token/bad-signature)
- [x] Rate limiting (in-memory для MVP)
- [x] Graceful shutdown + health-проверки
- [x] Метрики: дефолтные Go + RED по HTTP

## 3. Нефункциональные требования

| Требование | Значение | Статус |
|------------|----------|--------|
| Покрытие тестами | ≥ 70%, gate в CI | ✅ зафиксировано |
| Реплики | 1 (MVP), масштабирование — пост-MVP | предложено |
| Нагрузка (RPS, latency) | ? | **не обсуждалось** |
| SLA / доступность | ? | **не обсуждалось** |

## 4. Объём работ
----------------------------------------
Факты в финальном ТЗ (✓ сохранён / ✗ потерян):
  ✗ order-svc
  ✓ Go
  ✗ Postgres
  ✗ Kafka
  ✓ rate limit 100rps
  ✓ JWT 15m
  ✓ Kubernetes
  ✓ Prometheus
  ✓ дедлайн 1 марта
  ✓ тесты 70%
Стабильность: 7 из 10.
Токены: Σinput=19597, Σoutput=11008, стоимость $0.37318

# Стратегия 2: Sticky Facts
Финальное ТЗ (полный ответ модели):
----------------------------------------
# Техническое задание: order-svc

## 1. Общее описание

| Параметр | Значение |
|----------|----------|
| **Проект** | order-svc |
| **Тип** | Микросервис |
| **Цель** | Разработать микросервис управления заказами |
| **Язык** | Go |
| **Дедлайн MVP** | **1 марта** |

---

## 2. Технологический стек

| Компонент | Технология |
|-----------|-----------|
| **Язык** | Go |
| **База данных** | Postgres |
| **Очередь сообщений** | Kafka |
| **Авторизация** | JWT |
| **Оркестрация** | Kubernetes |
| **Метрики** | Prometheus |
| **Логирование** | Структурное JSON |

---

## 3. Функциональные требования

- **CRUD заказов** (ядро сервиса, Go + Postgres)
- **JWT-авторизация** — TTL токена 15 минут
- **Kafka** produce/consume (партиций ≥ 3, consumer-group)
- **Rate limiting** — 100 req/s **на клиента** (per-client throttling)

---

## 4. Нефункциональные требования

| Категория | Требование |
|-----------|-----------|
| **Надёжность** | 3 реплики в Kubernetes |
| **Логирование** | Структурное JSON |
| **Метрики** | Prometheus (RED + kafka_consumer_lag к MVP) |
| **Тестовое покрытие** | **Минимум 70%** |
| **Отказоустойчивость** | liveness/readiness probes, graceful shutdown |
| **Конфигурация** | ConfigMap + Secret |

---

## 5. Ограничения

- ⚠️ **JWT TTL — 15 минут**
- ⚠️ **Per-client throttling — 100 req/s**
- ⚠️ **Дедлайн — 1 марта**
- ⚠️ **Покрытие тестами — не ниже 70%**

---

## 6. Приоритизация под дедлайн (1 марта)

### 🔴 Must-have
- Базовый CRUD (Go + Postgres)
- JWT-авторизация (TTL 15 мин)
- Kafka produce/consume (партиций ≥ 3)
- **Distributed rate limiting** (per-client, на 3 репликах)
- liveness/readiness probes
- Graceful shutdown (коммит оффсетов Kafka)
- ConfigMap + Secret
- Структурное JSON-логирование
- Тесты ≥ 70%

### 🟡 Should-have (сразу после MVP)
- Полный набор Prometheus-метрик (к MVP — хотя бы RED + kafka_consumer_lag)
- PodDisruptionBudget
- startup probe
- Алерты по метрикам

### 🟢 Nice-to-have (после 1 марта)
- Exemplars (метрики ↔ трейсы)
- Тонкая настройка `max
----------------------------------------
Факты в финальном ТЗ (✓ сохранён / ✗ потерян):
  ✓ order-svc
  ✓ Go
  ✓ Postgres
  ✓ Kafka
  ✓ rate limit 100rps
  ✓ JWT 15m
  ✓ Kubernetes
  ✓ Prometheus
  ✓ дедлайн 1 марта
  ✓ тесты 70%
Стабильность: 10 из 10.
Токены: Σinput=19127, Σoutput=10262, стоимость $0.31443 (накладные стратегии: 2213 in + 1445 out = $0.00944)

  Накопленные facts (key-value):
    - deployment: Kubernetes
    - jwt_ttl: 15 минут
    - rate_limiting: 100 запросов в секунду на клиента
    - авторизация: JWT
    - база_данных: Postgres
    - дедлайн_mvp: 1 марта
    - логирование: структурное JSON
    - метрики: Prometheus
    - ограничение_запросов: per-client throttling
    - ограничения: JWT TTL 15 минут, per-client throttling, дедлайн 1 марта
    - оркестрация: Kubernetes
    - очередь_сообщений: Kafka
    - проект: order-svc
    - реплики: 3
    - стек: Go, Kubernetes, Postgres, Kafka, Prometheus, JWT
    - тестовое_покрытие: минимум 70%
    - тип: сервис
    - требования: JWT авторизация, структурное логирование, rate limiting 100 req/s, 3 реплики, минимум 70% покрытие тестами
    - цель: Разработать микросервис управления заказами
    - язык: Go

# Стратегия 3: Branching (ветки диалога)
  checkpoint после обсуждения базы; создаём 2 ветки от одного места.
  ветка postgres → В этой ветке мы выбрали **PostgreSQL** для order-svc.
  ветка mongo    → В этой ветке для order-svc вы выбрали **MongoDB**.
  ветки: [main postgres mongo] — каждая помнит свой выбор независимо.

=== Сравнение (посчитано из чисел выше) ===
стратегия       стабильность  Σinput  Σoutput  стоимость  накладные
Sliding Window  7/10 фактов   19597   11008    $0.37318   $0.00000
Sticky Facts    10/10 фактов  19127   10262    $0.31443   $0.00944

• Стабильность: Sticky Facts удержал на 3 факт(ов) больше (10/10 против 7/10) — важное лежит в facts и не выпадает из окна.
• Input: Sliding Window=19597, Sticky Facts=19127 (разница 2%). Окно шлёт меньше контекста; facts добавляет блок фактов + накладные на их обновление.
• Стоимость определяет output: у Sliding Window это ~74% суммы ($0.27520 из $0.37318). Длину ответов стратегия не контролирует, поэтому по ИТОГОВОЙ стоимости стратегии близки ($0.37318 vs $0.31443) — иногда «дешёвое» окно выходит даже дороже из-за разброса длины ответов.
• Вывод: контекст-стратегия влияет на INPUT и СТАБИЛЬНОСТЬ, а не на общий счёт —
  счёт правит длина ответов (output). Sliding Window — минимум контекста, но забывчив;
  Sticky Facts — помнит главное ценой небольших накладных; Branching — про развилки.
```

---

## Сравнение по критериям задания

> По реальному прогону (числа в примере выше).

**Качество / стабильность — главное различие.** Sliding Window при окне N=4 теряет
ранний стек (order-svc, Postgres, Kafka вышли за окно) — **7/10** фактов. Важная
деталь: модель не выдумала потерянное, а честно пометила «не обсуждали → открытые
вопросы» — поведение хорошее, но ТЗ всё равно неполное. Sticky Facts держит всё в
`facts` и собирает ТЗ целиком — **10/10**.

**Расход токенов.** Итоговая стоимость почти целиком определяется **output**
(длиной ответов): у Sliding Window это ~**74%** суммы. Длину ответов стратегия
**не контролирует**, поэтому общий счёт сильно зависит от того, насколько развёрнуто
ответила модель. На этом прогоне Sticky Facts вышел и стабильнее, и дешевле
($0.314 vs $0.373); на другом — наоборот окно было чуть дешевле по input. Стабильный
вывод один: **стратегия управляет input и памятью, а итоговым счётом правит output** —
хочешь резать стоимость, ограничивай длину ответов (`MaxTokens`/просьба отвечать кратко).

**Удобство для пользователя.** Sliding Window — просто и предсказуемо, но забывчиво
на длинной задаче (и честно об этом сообщает). Sticky Facts — помнит главное при
коротком окне, идеально для сбора ТЗ. Branching — удобно вести A/B-варианты от общей
точки, не смешивая их.

**Итог:** «лучшей» стратегии нет. Короткий чат → Sliding Window; долгая задача с
важными фактами → Sticky Facts; исследование вариантов → Branching.

## Файлы

- `strategy.go` — интерфейс `Strategy` и три реализации (Sliding Window, Sticky Facts, Branching).
- `agent.go` — агент с переключаемой стратегией и учётом токенов (диалог + накладные).
- `usage.go` — тарифы по моделям, `costFor`, типы сообщений.
- `report.go` — сценарий «собираем ТЗ», проверка стабильности, Branching-демо, сравнение.
- `main.go` — флаги `-report` / `-strategy`, команды веток в чате.

## Запуск

```bash
# .env должен содержать ANTHROPIC_API_KEY
go run . -report                 # сравнение стратегий
go run . -strategy facts         # чат: sliding | facts | branching
```