# 🔥 День 6. Первый агент

## Задание

Реализуйте простого агента, который:

- 👉 принимает запрос пользователя
- 👉 отправляет его в LLM через API
- 👉 получает ответ
- 👉 выводит результат в вашем интерфейсе

(простой чат, CLI или web, запросы через HTTP-клиент)

## Важно

- 👉 агент должен быть **отдельной сущностью**, а не просто один вызов API
- 👉 логика запроса и ответа должна быть **инкапсулирована в агенте**

## Результат

Агент принимает запрос и корректно вызывает LLM через API.

## Формат

Видео + Код

---

P.S. Видео: https://disk.yandex.ru/i/Yr-07wUn3xmVhg

## Реализация

CLI-агент на Go поверх Anthropic SDK.

- `agent.go` — сущность `Agent`: инкапсулирует клиента, конфиг (модель, `maxTokens`)
  и историю диалога. Наружу — один метод `Ask(ctx, input) (string, error)`.
- `main.go` — тонкий слой интерфейса (CLI): читает stdin, печатает stdout,
  доменной логики не содержит.